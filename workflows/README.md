# Workflows

Versioned AI workflow definitions for the FreelancifyHub ecosystem.

A workflow is a declarative description of a multi-step agent task (steps,
inputs, outputs, guards and rollbacks). Store workflows here as Markdown,
YAML or JSON as the ecosystem grows.

Naming convention: `workflows/<domain>-<workflow-name>.<ext>`.
