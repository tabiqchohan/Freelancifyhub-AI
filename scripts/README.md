# Scripts

Utility scripts for the FreelancifyHub AI ecosystem.

Scripts run against TypeScript sources directly via `tsx`.

- `validate-env.ts` — validates the current environment against the Zod schema
  (`npm run validate:env`).
