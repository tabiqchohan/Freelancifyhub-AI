# Tools

Tool catalogue for the FreelancifyHub AI ecosystem.

Tools are the sandboxed capabilities agents are allowed to invoke (search,
databases, external APIs, content generation). Keep the catalogue small,
audited and permission-scoped.

Recommended layout: one file per tool describing its contract, inputs,
outputs and required permissions.
